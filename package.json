{
  "name": "mycron",
  "version": "0.0.0",
  "private": true,
  "description": "MyCron — user-owned control plane for agent-scheduled actions. This package is the typecheck-only React+TypeScript UI contract; no application stack is committed yet.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "typescript": "^5.6.3"
  }
}
